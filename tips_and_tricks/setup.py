from setuptools import setup, find_packages

setup(
    name='TT_CLI_Tool',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[],
    author='David Parra',
    author_email='david_parra@epam.com',
    description='A sample package for the tip and tricks section',
)